# Configuration de l'API Laravel Sanctum
# Copiez ce fichier vers .env.local et ajustez les valeurs

# URL de base de l'API Laravel
VITE_API_URL=http://localhost:8000/api

# Domaines autorisés pour Sanctum (cookies)
VITE_SANCTUM_STATEFUL_DOMAINS=localhost:3000,localhost:5173,127.0.0.1:3000

# Configuration optionnelle
VITE_APP_NAME=Coovia
VITE_APP_ENV=local

# URLs de redirection (optionnel)
VITE_LOGIN_REDIRECT=/dashboard
VITE_LOGOUT_REDIRECT=/login
